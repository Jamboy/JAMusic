<!-- pages/video/video.wxml -->
<view class="video">
  <block wx:for="{{topMvs}}" wx:for-item="item" wx:key="id">
    <video-card item="{{item}}" class="video-card" bind:click="onClickItem" />
  </block>
</view>
